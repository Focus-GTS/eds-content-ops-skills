# Changelog

## 1.0.0

### Added

- Initial release
- Boilerplate currency detection and version comparison
- Core file modification detection (aem.js, aem.css integrity check)
- CSS scoping audit across all blocks
- Block structure validation with per-block health cards
- Performance pattern detection (E-L-D phase compliance, eager bloat, render-blocking resources)
- Configuration health checks (fstab.yaml, helix-query.yaml, head.html, ESLint)
- Weighted composite project health grade (A-F)
